pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
              
                    batch 'mvn clean compile'
                
            }
        }

    }
}
